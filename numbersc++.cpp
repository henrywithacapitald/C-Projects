#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    cout << pow(2, 5);  to the power of
    cout << sqrt(20); square root
    cout << round(4.3); rounds clowest
    cout << celi(4.6); rounds up
    cout << floor(4.8); rounds down
    cout << fmax(3, 10); which one the biggest
    cout << fmin(3, 10); which one is the smallest
    return 0;
}
